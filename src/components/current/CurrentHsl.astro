<section class="current-hsl" data-current-hsl aria-labelledby="current-hsl-title">
  <header class="hsl-heading">
    <p id="current-hsl-title" class="hsl-index">HSL / YLISRINNE</p>
    <p class="hsl-mode">121 + 125 / KAMPPI + TAPIOLA</p>
  </header>

  <div class="hsl-meta">
    <p data-hsl-status>YLISRINNE / E3239 / FETCHING</p>
    <p>REFRESH / 15 S</p>
  </div>

  <div class="hsl-results" data-hsl-results hidden>
    <div class="hsl-results__header" aria-hidden="true">
      <span>LINE</span>
      <span>DESTINATION</span>
      <span>DEPARTURE</span>
      <span>DATA</span>
    </div>
    <div class="hsl-departures" data-hsl-departures aria-live="polite"></div>
  </div>

  <section class="hsl-learning" data-hsl-learning aria-label="Aapo ETA model performance" hidden>
    <header class="hsl-learning__header">
      <span>AAPO ETA / SCOREBOARD</span>
      <span data-hsl-learning-state>LEARNING</span>
    </header>
    <div class="hsl-learning__stats">
      <div>
        <span>ARRIVALS</span>
        <strong data-hsl-learning-arrivals>0</strong>
      </div>
      <div>
        <span>HSL MAE</span>
        <strong data-hsl-learning-hsl-mae>--</strong>
      </div>
      <div>
        <span>AAPO MAE</span>
        <strong data-hsl-learning-model-mae>--</strong>
      </div>
      <div>
        <span>AAPO WINS</span>
        <strong data-hsl-learning-wins>--</strong>
      </div>
    </div>
    <p class="hsl-learning__meta" data-hsl-learning-meta></p>
  </section>

  <p class="hsl-message" data-hsl-empty hidden>NO 121 OR 125 DEPARTURES.</p>
  <p class="hsl-message hsl-message--error" data-hsl-error role="status" hidden></p>

  <footer class="hsl-footer">
    <p>
      DATA /
      <a href="https://digitransit.fi/" target="_blank" rel="noopener">HSL DIGITRANSIT</a>
      · GPS / HSL GTFS-RT · AAPO ETA / HISTORICAL RESIDUAL MODEL · AUTO REFRESH 15 S
    </p>
  </footer>
</section>

<script>
  import { initCurrentHsl } from '../../features/current/hsl';
  initCurrentHsl();
</script>

<style is:global>
  .current-hsl {
    --hsl-rule: color-mix(in srgb, var(--line-soft) 52%, transparent);
    padding: 30px 0 8px;
  }

  .hsl-heading,
  .hsl-meta,
  .hsl-results__header,
  .hsl-footer,
  .hsl-departure,
  .hsl-learning {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  }

  .hsl-heading {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 24px;
    margin-bottom: 24px;
  }

  .hsl-index,
  .hsl-mode,
  .hsl-meta p,
  .hsl-footer p,
  .hsl-departure p,
  .hsl-learning p {
    margin: 0;
  }

  .hsl-index,
  .hsl-mode {
    font-size: 0.56rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .hsl-index {
    color: var(--moss);
  }

  .hsl-mode {
    color: var(--stone-light);
  }

  .hsl-meta {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 20px;
    padding: 13px 0;
    border-top: 1px solid var(--hsl-rule);
    border-bottom: 1px solid var(--hsl-rule);
    color: var(--stone-light);
    font-size: 0.43rem;
    font-weight: 500;
    line-height: 1.2;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .hsl-meta p:first-child {
    color: var(--stone);
  }

  .hsl-results {
    padding-top: 4px;
  }

  .hsl-results__header,
  .hsl-departure {
    display: grid;
    grid-template-columns: 70px minmax(0, 1fr) 132px 70px;
    gap: 14px;
    align-items: center;
  }

  .hsl-results__header {
    padding: 13px 0 9px;
    color: var(--stone-light);
    font-size: 0.4rem;
    font-weight: 600;
    letter-spacing: 0.065em;
    text-transform: uppercase;
  }

  .hsl-departure {
    min-height: 72px;
    padding: 13px 0;
    border-top: 1px solid var(--hsl-rule);
  }

  .hsl-departure--previous {
    opacity: 0.64;
  }

  .hsl-departure--first-upcoming {
    border-top-color: var(--line);
  }

  .hsl-departure__line {
    color: var(--ink);
    font-family: "Times New Roman", Times, serif !important;
    font-size: 1.55rem;
    line-height: 1;
    letter-spacing: -0.025em;
  }

  .hsl-departure__destination-wrap {
    display: grid;
    min-width: 0;
    gap: 4px;
  }

  .hsl-departure__destination {
    min-width: 0;
    overflow: hidden;
    color: var(--ink-soft);
    font-size: 0.68rem;
    line-height: 1.25;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .hsl-departure__vehicle {
    min-width: 0;
    overflow: hidden;
    color: var(--moss);
    font-size: 0.41rem;
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: 0.045em;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .hsl-departure__time {
    display: grid;
    grid-template-columns: auto auto;
    justify-content: start;
    gap: 3px 9px;
    align-items: baseline;
  }

  .hsl-departure__countdown {
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-size: 1.18rem;
    line-height: 1;
    white-space: nowrap;
  }

  .hsl-departure__clock,
  .hsl-departure__delay {
    color: var(--stone-light);
    font-size: 0.42rem;
    font-weight: 600;
    letter-spacing: 0.045em;
    white-space: nowrap;
  }

  .hsl-departure__delay {
    grid-column: 1 / -1;
    color: var(--stone);
  }

  .hsl-departure__model {
    display: flex;
    grid-column: 1 / -1;
    gap: 7px;
    align-items: baseline;
    color: var(--moss);
    font-size: 0.4rem;
    font-weight: 700;
    letter-spacing: 0.045em;
    white-space: nowrap;
  }

  .hsl-departure__model span:last-child {
    color: var(--stone-light);
    font-size: 0.34rem;
  }

  .hsl-departure__state-wrap {
    display: grid;
    justify-items: end;
    gap: 3px;
  }

  .hsl-departure__state {
    color: var(--stone-light);
    font-size: 0.43rem;
    font-weight: 700;
    letter-spacing: 0.07em;
    text-align: right;
  }

  .hsl-departure__state--live {
    color: var(--moss);
  }

  .hsl-departure__state-age {
    color: var(--stone-light);
    font-size: 0.35rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    white-space: nowrap;
  }

  .hsl-learning {
    margin-top: 18px;
    border-top: 1px solid var(--hsl-rule);
    border-bottom: 1px solid var(--hsl-rule);
  }

  .hsl-learning__header {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    padding: 12px 0 9px;
    color: var(--stone);
    font-size: 0.42rem;
    font-weight: 700;
    letter-spacing: 0.065em;
    text-transform: uppercase;
  }

  .hsl-learning__header span:last-child {
    color: var(--moss);
  }

  .hsl-learning__stats {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 18px;
    padding: 9px 0 13px;
  }

  .hsl-learning__stats div {
    display: grid;
    gap: 5px;
  }

  .hsl-learning__stats span {
    color: var(--stone-light);
    font-size: 0.35rem;
    font-weight: 600;
    letter-spacing: 0.055em;
  }

  .hsl-learning__stats strong {
    color: var(--ink-soft);
    font-family: "Times New Roman", Times, serif;
    font-size: 1.05rem;
    font-weight: 400;
    line-height: 1;
  }

  .hsl-learning__meta {
    padding: 0 0 12px;
    color: var(--stone-light);
    font-size: 0.34rem;
    font-weight: 600;
    letter-spacing: 0.045em;
    text-transform: uppercase;
  }

  .hsl-message {
    margin: 22px 0 0;
    color: var(--stone);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.5rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .hsl-message--error {
    color: var(--ink-soft);
  }

  .hsl-footer {
    margin-top: 22px;
    padding-top: 15px;
    border-top: 1px solid var(--hsl-rule);
    color: var(--stone-light);
    font-size: 0.43rem;
    font-weight: 500;
    line-height: 1.5;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .hsl-footer a {
    color: var(--stone);
    text-decoration: none;
  }

  .hsl-footer a:hover,
  .hsl-footer a:focus-visible {
    color: var(--moss-deep);
  }

  @media (max-width: 640px) {
    .current-hsl {
      padding-top: 26px;
    }

    .hsl-heading {
      align-items: flex-start;
      margin-bottom: 19px;
    }

    .hsl-mode {
      max-width: 180px;
      line-height: 1.35;
      text-align: right;
    }

    .hsl-meta {
      font-size: 0.39rem;
    }

    .hsl-results__header,
    .hsl-departure {
      grid-template-columns: 48px minmax(0, 1fr) 86px 48px;
      gap: 8px;
    }

    .hsl-results__header {
      font-size: 0.34rem;
      letter-spacing: 0.04em;
    }

    .hsl-departure {
      min-height: 64px;
      padding: 11px 0;
    }

    .hsl-departure__line {
      font-size: 1.27rem;
    }

    .hsl-departure__destination {
      font-size: 0.58rem;
    }

    .hsl-departure__vehicle {
      font-size: 0.32rem;
      letter-spacing: 0.025em;
    }

    .hsl-departure__time {
      gap: 2px 5px;
    }

    .hsl-departure__countdown {
      font-size: 0.92rem;
    }

    .hsl-departure__clock,
    .hsl-departure__delay,
    .hsl-departure__state {
      font-size: 0.34rem;
    }

    .hsl-departure__model {
      gap: 4px;
      font-size: 0.31rem;
      letter-spacing: 0.02em;
    }

    .hsl-departure__model span:last-child,
    .hsl-departure__state-age {
      font-size: 0.29rem;
    }

    .hsl-learning__stats {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 14px 18px;
    }

    .hsl-learning__stats strong {
      font-size: 0.95rem;
    }
  }
</style>
