<section class="current-weather" data-current-weather aria-labelledby="current-weather-title">
  <header class="weather-heading">
    <p class="weather-index">WEATHER / 01</p>
    <p class="weather-location">OLARI / ESPOO</p>
  </header>

  <div class="weather-current">
    <p class="weather-temperature" aria-live="polite">
      <span data-weather-temperature>--.-</span><span class="weather-temperature__unit">°C</span>
    </p>

    <div class="weather-current__visual">
      <div class="weather-current__icon" data-weather-current-icon aria-hidden="true"></div>

      <h2 id="current-weather-title" class="weather-condition" data-weather-condition>
        Fetching forecast
      </h2>

      <p class="weather-current__range" aria-label="Today's low and high temperature">
        <span data-weather-current-min>--</span>° / <span data-weather-current-max>--</span>°
      </p>

      <div class="weather-current__solar" aria-label="Sunrise, sunset and daylight length">
        <p>SUN <span data-weather-sunrise>--:--</span> - <span data-weather-sunset>--:--</span></p>
        <p>DAYLIGHT <span data-weather-daylight>--</span></p>
      </div>
    </div>
  </div>

  <section class="weather-hours" aria-label="Next six hours">
    <div class="weather-hour-grid" data-weather-hours aria-live="polite"></div>
  </section>

  <section class="weather-days" aria-label="Next five days">
    <div class="weather-day-grid" data-weather-days aria-live="polite"></div>

    <div class="weather-daily-chart-frame">
      <svg
        class="weather-daily-chart"
        data-weather-daily-chart
        role="img"
        aria-label="High and low temperature development for the next five days"
      ></svg>
    </div>
  </section>

  <footer class="weather-footer">
    <p>
      DATA /
      <a href="https://open-meteo.com/" target="_blank" rel="noopener">OPEN-METEO</a>
      · CC BY 4.0 · AUTO REFRESH 15 MIN
    </p>

    <p class="weather-error" data-weather-error hidden>
      Forecast unavailable.
      <button type="button" data-weather-retry>Retry</button>
    </p>
  </footer>
</section>

<script>
  import { initCurrentWeather } from '../../features/current/weather';
  initCurrentWeather();
</script>

<style>
  .current-weather {
    padding: 30px 0 8px;
  }

  .weather-heading {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 24px;
    margin-bottom: 24px;
  }

  .weather-index,
  .weather-location,
  .weather-current__range,
  .weather-current__solar,
  .weather-footer {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    text-transform: uppercase;
  }

  .weather-index,
  .weather-location {
    margin: 0;
    font-size: 0.56rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.09em;
  }

  .weather-index {
    color: var(--moss);
  }

  .weather-location {
    color: var(--stone-light);
  }

  .weather-current {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(150px, 0.34fr);
    align-items: center;
    gap: clamp(24px, 6vw, 72px);
    padding: 8px 0 30px;
  }

  .weather-temperature {
    margin: 0;
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-size: clamp(4.8rem, 10vw, 7.5rem);
    font-weight: 400;
    line-height: 0.82;
    letter-spacing: -0.055em;
  }

  .weather-temperature__unit {
    margin-left: 0.08em;
    color: var(--stone-light);
    font-size: 0.31em;
    letter-spacing: -0.02em;
    vertical-align: top;
  }

  .weather-current__visual {
    min-width: 0;
    display: grid;
    justify-items: center;
    text-align: center;
  }

  .weather-current__icon {
    width: clamp(46px, 6vw, 62px);
    height: clamp(46px, 6vw, 62px);
    color: var(--moss-deep);
  }

  .weather-current__icon :global(.weather-symbol),
  .weather-hour-grid :global(.weather-symbol),
  .weather-day-grid :global(.weather-symbol) {
    display: block;
    width: 100%;
    height: 100%;
    overflow: visible;
  }

  .weather-condition,
  .weather-current__range,
  .weather-current__solar p {
    margin: 0;
  }

  .weather-condition {
    margin-top: 10px;
    color: var(--ink-soft);
    font-size: 0.82rem;
    font-weight: 400;
    line-height: 1.35;
    letter-spacing: -0.004em;
  }

  .weather-current__range {
    margin-top: 5px;
    color: var(--stone);
    font-size: 0.54rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.065em;
  }

  .weather-current__solar {
    display: grid;
    gap: 4px;
    margin-top: 13px;
    color: var(--stone-light);
    font-size: 0.43rem;
    font-weight: 500;
    line-height: 1.2;
    letter-spacing: 0.055em;
  }

  .weather-hours,
  .weather-days {
    border-top: 1px solid var(--line-soft);
  }

  .weather-hours {
    padding: 20px 0 22px;
  }

  .weather-days {
    padding: 20px 0 6px;
  }

  .weather-hour-grid,
  .weather-day-grid {
    display: grid;
    gap: 0;
  }

  .weather-hour-grid {
    grid-template-columns: repeat(6, minmax(0, 1fr));
  }

  .weather-day-grid {
    grid-template-columns: repeat(5, minmax(0, 1fr));
  }

  .weather-hour-grid :global(.weather-hour),
  .weather-day-grid :global(.weather-day) {
    min-width: 0;
    text-align: center;
  }

  .weather-hour-grid :global(.weather-hour + .weather-hour),
  .weather-day-grid :global(.weather-day + .weather-day) {
    border-left: 1px solid color-mix(in srgb, var(--line-soft) 68%, transparent);
  }

  .weather-hour-grid :global(.weather-hour) {
    padding: 2px 8px;
  }

  .weather-day-grid :global(.weather-day) {
    padding: 2px 8px 6px;
  }

  .weather-hour-grid :global(.weather-hour__time),
  .weather-hour-grid :global(.weather-hour__rain),
  .weather-day-grid :global(.weather-day__name),
  .weather-day-grid :global(.weather-day__rain) {
    margin: 0;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    text-transform: uppercase;
  }

  .weather-hour-grid :global(.weather-hour__time),
  .weather-day-grid :global(.weather-day__name) {
    color: var(--stone);
    font-size: 0.5rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.055em;
  }

  .weather-hour-grid :global(.weather-hour__icon),
  .weather-day-grid :global(.weather-day__icon) {
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--moss-deep);
  }

  .weather-hour-grid :global(.weather-hour__icon) {
    width: 26px;
    height: 26px;
    margin: 11px auto 8px;
  }

  .weather-day-grid :global(.weather-day__icon) {
    width: 30px;
    height: 30px;
    margin: 11px auto 7px;
  }

  .weather-hour-grid :global(.weather-hour__temperature) {
    margin: 0;
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-size: 1.08rem;
    line-height: 1;
  }

  .weather-hour-grid :global(.weather-hour__rain),
  .weather-day-grid :global(.weather-day__rain) {
    margin-top: 7px;
    color: var(--stone-light);
    font-size: 0.41rem;
    font-weight: 500;
    line-height: 1;
    letter-spacing: 0.035em;
  }

  .weather-daily-chart-frame {
    width: 100%;
    min-height: 116px;
    padding-top: 2px;
  }

  .weather-daily-chart {
    display: block;
    width: 100%;
    height: 116px;
    overflow: visible;
  }

  .weather-footer {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 24px;
    padding-top: 15px;
    border-top: 1px solid var(--line-soft);
    color: var(--stone-light);
    font-size: 0.43rem;
    font-weight: 500;
    line-height: 1.5;
    letter-spacing: 0.06em;
  }

  .weather-footer p {
    margin: 0;
  }

  .weather-footer a,
  .weather-error button {
    color: var(--stone);
  }

  .weather-footer a {
    text-decoration: none;
  }

  .weather-footer a:hover,
  .weather-footer a:focus-visible,
  .weather-error button:hover,
  .weather-error button:focus-visible {
    color: var(--moss-deep);
  }

  .weather-error button {
    min-width: 24px;
    min-height: 24px;
    padding: 0 2px;
    border: 0;
    background: transparent;
    font: inherit;
    letter-spacing: inherit;
    text-transform: inherit;
    text-decoration: underline;
    cursor: pointer;
  }

  @media (max-width: 640px) {
    .current-weather {
      padding-top: 26px;
    }

    .weather-heading {
      margin-bottom: 19px;
    }

    .weather-current {
      grid-template-columns: minmax(0, 1fr) minmax(105px, 0.42fr);
      gap: 12px;
      padding: 4px 0 25px;
    }

    .weather-temperature {
      font-size: clamp(4.5rem, 25vw, 6.1rem);
    }

    .weather-current__icon {
      width: 48px;
      height: 48px;
    }

    .weather-condition {
      margin-top: 8px;
      font-size: 0.72rem;
    }

    .weather-current__range {
      font-size: 0.48rem;
    }

    .weather-current__solar {
      gap: 3px;
      margin-top: 10px;
      font-size: 0.37rem;
      letter-spacing: 0.04em;
    }

    .weather-hours,
    .weather-days {
      padding-top: 17px;
    }

    .weather-hours {
      padding-bottom: 19px;
    }

    .weather-hour-grid :global(.weather-hour) {
      padding-left: 4px;
      padding-right: 4px;
    }

    .weather-day-grid :global(.weather-day) {
      padding-left: 4px;
      padding-right: 4px;
    }

    .weather-hour-grid :global(.weather-hour__time),
    .weather-day-grid :global(.weather-day__name) {
      font-size: 0.43rem;
      letter-spacing: 0.035em;
    }

    .weather-hour-grid :global(.weather-hour__icon) {
      width: 23px;
      height: 23px;
      margin-top: 10px;
    }

    .weather-day-grid :global(.weather-day__icon) {
      width: 27px;
      height: 27px;
      margin-top: 10px;
    }

    .weather-hour-grid :global(.weather-hour__temperature) {
      font-size: 0.98rem;
    }

    .weather-hour-grid :global(.weather-hour__rain),
    .weather-day-grid :global(.weather-day__rain) {
      font-size: 0.36rem;
    }

    .weather-daily-chart-frame,
    .weather-daily-chart {
      min-height: 108px;
      height: 108px;
    }

    .weather-footer {
      display: block;
      padding-top: 13px;
      font-size: 0.39rem;
    }

    .weather-error {
      margin-top: 10px !important;
    }
  }
</style>
