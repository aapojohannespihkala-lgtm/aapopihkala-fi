<section class="current-news" data-current-news aria-labelledby="current-news-title">
  <header class="news-heading">
    <p class="news-index">NEWS / 03</p>
    <p class="news-mode">LOCAL PROTOTYPE / SEED POOL</p>
  </header>

  <div class="news-intro">
    <h2 id="current-news-title">Three for today</h2>
    <p>Rate each item. Both choices replace it, but teach the local profile in opposite directions.</p>
  </div>

  <div class="news-list" data-news-list>
    {[0, 1, 2].map((index) => (
      <article class="news-item" data-news-slot data-slot-index={index}>
        <p class="news-item__number" aria-hidden="true">{String(index + 1).padStart(2, '0')}</p>

        <div class="news-item__body">
          <p class="news-item__meta" data-news-meta>LOADING</p>
          <h3 class="news-item__title" data-news-title>Loading prototype item</h3>
          <p class="news-item__detail" data-news-detail>LOCAL / PROTOTYPE</p>
        </div>

        <div class="news-item__feedback" aria-label={`Feedback for news slot ${index + 1}`}>
          <button
            type="button"
            class="news-feedback"
            data-news-feedback="up"
            aria-label="Thumbs up, show another item"
            title="Thumbs up - show another item"
          >↑</button>
          <button
            type="button"
            class="news-feedback"
            data-news-feedback="down"
            aria-label="Thumbs down, show another item"
            title="Thumbs down - show another item"
          >↓</button>
        </div>
      </article>
    ))}
  </div>

  <p class="sr-only" aria-live="polite" data-news-feedback-status></p>

  <details class="news-debug">
    <summary>LOCAL LEARNING / DEBUG</summary>

    <div class="news-debug__body">
      <dl class="news-debug__stats">
        <div><dt>POOL</dt><dd data-news-debug-pool>--</dd></div>
        <div><dt>REMAINING</dt><dd data-news-debug-remaining>--</dd></div>
        <div><dt>SEEN</dt><dd data-news-debug-seen>--</dd></div>
        <div><dt>UP</dt><dd data-news-debug-up>--</dd></div>
        <div><dt>DOWN</dt><dd data-news-debug-down>--</dd></div>
      </dl>

      <div class="news-debug__signals" data-news-debug-signals></div>

      <button type="button" class="news-debug__reset" data-news-reset>
        RESET LOCAL LEARNING
      </button>
    </div>
  </details>

  <footer class="news-footer">
    <p>PROFILE / LOCAL STORAGE</p>
    <p>FEEDS / NOT CONNECTED</p>
  </footer>
</section>

<style>
  .current-news {
    padding: 30px 0 6px;
  }

  .news-heading,
  .news-footer {
    display: flex;
    justify-content: space-between;
    gap: 20px;
  }

  .news-heading {
    margin-bottom: 18px;
  }

  .news-index,
  .news-mode,
  .news-item__number,
  .news-item__meta,
  .news-item__detail,
  .news-feedback,
  .news-debug,
  .news-footer {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    text-transform: uppercase;
  }

  .news-index,
  .news-mode {
    margin: 0;
    font-size: 0.56rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.09em;
  }

  .news-index {
    color: var(--moss);
  }

  .news-mode {
    color: var(--stone-light);
  }

  .news-intro {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(260px, 0.7fr);
    gap: 42px;
    align-items: end;
    padding: 4px 0 18px;
  }

  .news-intro h2,
  .news-intro p {
    margin: 0;
  }

  .news-intro h2 {
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-size: clamp(2.2rem, 5vw, 4.6rem);
    font-weight: 400;
    line-height: 0.9;
    letter-spacing: -0.035em;
    text-transform: none;
  }

  .news-intro p {
    max-width: 46ch;
    color: var(--stone);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.53rem;
    line-height: 1.55;
    letter-spacing: 0.055em;
    text-transform: uppercase;
  }

  .news-list {
    border-top: 1px solid var(--line);
  }

  .news-item {
    display: grid;
    grid-template-columns: 44px minmax(0, 1fr) auto;
    gap: 20px;
    align-items: center;
    min-height: 132px;
    padding: 20px 0;
    border-bottom: 1px solid var(--line-soft);
  }

  .news-item__number,
  .news-item__meta,
  .news-item__detail {
    margin: 0;
    font-size: 0.5rem;
    letter-spacing: 0.075em;
  }

  .news-item__number,
  .news-item__detail {
    color: var(--stone-light);
  }

  .news-item__meta {
    margin-bottom: 8px;
    color: var(--stone);
    font-weight: 600;
  }

  .news-item__title {
    margin: 0;
    max-width: 28ch;
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-size: clamp(1.35rem, 2.8vw, 2.25rem);
    font-weight: 400;
    line-height: 1.04;
    letter-spacing: -0.02em;
  }

  .news-item__detail {
    margin-top: 10px;
  }

  .news-item__feedback {
    display: flex;
    gap: 6px;
  }

  .news-feedback {
    display: grid;
    width: 42px;
    height: 42px;
    place-items: center;
    border: 1px solid var(--line);
    border-radius: 999px;
    background: transparent;
    color: var(--stone);
    font-size: 1rem;
    line-height: 1;
    cursor: pointer;
  }

  .news-feedback:hover,
  .news-feedback:focus-visible {
    border-color: var(--stone);
    color: var(--ink);
  }

  .news-feedback:disabled {
    cursor: default;
    opacity: 0.3;
  }

  .news-debug {
    margin-top: 28px;
    border-top: 1px solid var(--line-soft);
    border-bottom: 1px solid var(--line-soft);
    color: var(--stone-light);
    font-size: 0.49rem;
    letter-spacing: 0.07em;
  }

  .news-debug summary {
    padding: 13px 0;
    cursor: pointer;
  }

  .news-debug__body {
    padding: 4px 0 18px;
  }

  .news-debug__stats {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 12px;
    margin: 0;
  }

  .news-debug__stats div {
    display: grid;
    gap: 5px;
    padding-top: 10px;
    border-top: 1px solid var(--line-soft);
  }

  .news-debug__stats dt,
  .news-debug__stats dd {
    margin: 0;
  }

  .news-debug__stats dd {
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-size: 1.2rem;
    line-height: 1;
  }

  .news-debug__signals {
    display: grid;
    gap: 6px;
    margin-top: 18px;
  }

  :global(.news-debug__signal) {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    margin: 0;
  }

  :global(.news-debug__empty) {
    margin: 0;
  }

  .news-debug__reset {
    margin-top: 18px;
    border: 0;
    padding: 0;
    background: none;
    color: inherit;
    font: inherit;
    text-decoration: underline;
    text-underline-offset: 0.18em;
    cursor: pointer;
  }

  .news-footer {
    margin-top: 24px;
    color: var(--stone-light);
    font-size: 0.49rem;
    letter-spacing: 0.07em;
  }

  .news-footer p {
    margin: 0;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (max-width: 720px) {
    .news-heading,
    .news-footer {
      align-items: flex-start;
      flex-direction: column;
      gap: 7px;
    }

    .news-intro {
      grid-template-columns: 1fr;
      gap: 14px;
    }

    .news-item {
      grid-template-columns: 30px minmax(0, 1fr);
      gap: 14px;
      min-height: 0;
    }

    .news-item__feedback {
      grid-column: 2;
      justify-content: flex-start;
    }

    .news-debug__stats {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }
</style>
