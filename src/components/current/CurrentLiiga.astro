---
import { LIIGA_TEAMS } from '../../config/liigaTeams';
import { getLiigaMarkPaths } from '../../config/liigaMarks';
---

<section class="current-liiga" data-current-liiga aria-labelledby="current-liiga-title" aria-busy="true">
  <svg class="liiga-mark-sprite" aria-hidden="true" focusable="false">
    <defs>
      {
        LIIGA_TEAMS.map((team) => (
          <symbol id={`liiga-match-mark-${team.id}`} viewBox="0 0 48 48">
            {
              getLiigaMarkPaths(team.id).map((path, pathIndex) => (
                <path
                  d={path}
                  class={pathIndex === 0 ? 'liiga-team-mark__shape' : 'liiga-team-mark__shape liiga-team-mark__detail'}
                ></path>
              ))
            }
          </symbol>
        ))
      }
    </defs>
  </svg>

  <header class="liiga-heading">
    <div>
      <p class="liiga-index">LIIGA / <span data-liiga-season>--</span></p>
      <h2 id="current-liiga-title">Finnish Liiga</h2>
    </div>
    <p class="liiga-source-state" data-liiga-status>LOADING / LIIGA</p>
  </header>

  <section class="liiga-ilves" aria-labelledby="liiga-ilves-title">
    <div class="liiga-panel-heading">
      <p id="liiga-ilves-title">ILVES / STATUS</p>
      <p data-liiga-ilves-position>#-- / 17</p>
    </div>

    <article class="liiga-live-card" data-liiga-live-card hidden aria-live="polite">
      <div class="liiga-live-card__header">
        <span>LIVE / ILVES</span>
        <span data-liiga-live-clock>LIVE</span>
      </div>

      <p class="liiga-visually-hidden" data-liiga-live-matchup>--</p>

      <div class="liiga-match-board liiga-match-board--live">
        <div class="liiga-match-team" data-liiga-live-home-side>
          <span class="liiga-match-team__name" data-liiga-live-home-team>--</span>
          <svg class="liiga-match-team__mark" viewBox="0 0 48 48" aria-hidden="true" hidden>
            <use data-liiga-live-home-mark href="#liiga-match-mark-ilves"></use>
          </svg>
        </div>

        <p class="liiga-match-center liiga-live-card__score" data-liiga-live-score>0 - 0</p>

        <div class="liiga-match-team liiga-match-team--away" data-liiga-live-away-side>
          <svg class="liiga-match-team__mark" viewBox="0 0 48 48" aria-hidden="true" hidden>
            <use data-liiga-live-away-mark href="#liiga-match-mark-ilves"></use>
          </svg>
          <span class="liiga-match-team__name" data-liiga-live-away-team>--</span>
        </div>
      </div>
    </article>

    <div class="liiga-status-grid">
      <article class="liiga-status-card liiga-status-card--match">
        <div class="liiga-status-card__header">
          <span>LAST GAME</span>
          <span data-liiga-last-date>--</span>
        </div>

        <p class="liiga-visually-hidden" data-liiga-last-matchup>--</p>

        <div class="liiga-match-board">
          <div class="liiga-match-team" data-liiga-last-home-side>
            <span class="liiga-match-team__name" data-liiga-last-home-team>--</span>
            <svg class="liiga-match-team__mark" viewBox="0 0 48 48" aria-hidden="true" hidden>
              <use data-liiga-last-home-mark href="#liiga-match-mark-ilves"></use>
            </svg>
          </div>

          <p class="liiga-match-center" data-liiga-last-score>-- - --</p>

          <div class="liiga-match-team liiga-match-team--away" data-liiga-last-away-side>
            <svg class="liiga-match-team__mark" viewBox="0 0 48 48" aria-hidden="true" hidden>
              <use data-liiga-last-away-mark href="#liiga-match-mark-ilves"></use>
            </svg>
            <span class="liiga-match-team__name" data-liiga-last-away-team>--</span>
          </div>
        </div>

        <p class="liiga-status-card__meta liiga-status-card__meta--match">
          <span data-liiga-last-audience>ATTENDANCE --</span>
          <span aria-hidden="true">/</span>
          <span data-liiga-last-result>WAITING</span>
        </p>
      </article>

      <article class="liiga-status-card liiga-status-card--match">
        <div class="liiga-status-card__header">
          <span>NEXT GAME</span>
          <span data-liiga-next-venue>--</span>
        </div>

        <p class="liiga-visually-hidden" data-liiga-next-matchup>--</p>

        <div class="liiga-match-board">
          <div class="liiga-match-team" data-liiga-next-home-side>
            <span class="liiga-match-team__name" data-liiga-next-home-team>--</span>
            <svg class="liiga-match-team__mark" viewBox="0 0 48 48" aria-hidden="true" hidden>
              <use data-liiga-next-home-mark href="#liiga-match-mark-ilves"></use>
            </svg>
          </div>

          <p class="liiga-match-center liiga-match-center--vs">VS</p>

          <div class="liiga-match-team liiga-match-team--away" data-liiga-next-away-side>
            <svg class="liiga-match-team__mark" viewBox="0 0 48 48" aria-hidden="true" hidden>
              <use data-liiga-next-away-mark href="#liiga-match-mark-ilves"></use>
            </svg>
            <span class="liiga-match-team__name" data-liiga-next-away-team>--</span>
          </div>
        </div>

        <p class="liiga-status-card__kickoff" data-liiga-next-date>--</p>

        <div class="liiga-next-home" data-liiga-next-home hidden>
          <span>NEXT HOME</span>
          <strong data-liiga-next-home-opponent>--</strong>
          <span data-liiga-next-home-date>--</span>
        </div>
      </article>

      <article class="liiga-status-card liiga-status-card--position">
        <div class="liiga-status-card__header">
          <span>POSITION</span>
          <span>REGULAR SEASON</span>
        </div>
        <p class="liiga-status-card__position" data-liiga-position>-- / --</p>
        <p class="liiga-status-card__meta" data-liiga-position-meta>WAITING FOR STANDINGS</p>
      </article>
    </div>
  </section>

  <section class="liiga-standings-section" aria-labelledby="liiga-standings-title">
    <div class="liiga-panel-heading">
      <p id="liiga-standings-title">STANDINGS</p>
      <p>REGULAR SEASON</p>
    </div>

    <div class="liiga-table" role="table" aria-label="Liiga standings">
      <div class="liiga-row liiga-row--header" role="row">
        <span role="columnheader">#</span>
        <span role="columnheader">TEAM</span>
        <span role="columnheader">O</span>
        <span class="liiga-col-secondary" role="columnheader">V</span>
        <span class="liiga-col-secondary" role="columnheader">T</span>
        <span class="liiga-col-secondary" role="columnheader">H</span>
        <span class="liiga-col-secondary" role="columnheader">LP</span>
        <span role="columnheader">P</span>
        <span role="columnheader">ME</span>
      </div>

      <div data-liiga-standings>
        {
          LIIGA_TEAMS.map((team, index) => (
            <div class="liiga-row" role="row" data-liiga-row={team.id} data-liiga-rank={index + 1}>
              <span class="liiga-rank" role="cell" data-liiga-rank>{index + 1}</span>
              <span class="liiga-team" role="rowheader">
                <span class="liiga-team-mark" aria-hidden="true">
                  <svg viewBox="0 0 48 48" focusable="false" data-liiga-mark-id={team.id}>
                    {
                      getLiigaMarkPaths(team.id).map((path, pathIndex) => (
                        <path
                          d={path}
                          class={pathIndex === 0 ? 'liiga-team-mark__shape' : 'liiga-team-mark__shape liiga-team-mark__detail'}
                        ></path>
                      ))
                    }
                  </svg>
                </span>
                <span class="liiga-team__name">{team.name}</span>
              </span>
              <span role="cell" data-liiga-games>0</span>
              <span class="liiga-col-secondary" role="cell" data-liiga-wins>0</span>
              <span class="liiga-col-secondary" role="cell" data-liiga-ties>0</span>
              <span class="liiga-col-secondary" role="cell" data-liiga-losses>0</span>
              <span class="liiga-col-secondary" role="cell" data-liiga-bonus>0</span>
              <span class="liiga-points" role="cell" data-liiga-points>0</span>
              <span role="cell" data-liiga-difference>0</span>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <section class="liiga-clubs" aria-labelledby="liiga-clubs-title">
    <div class="liiga-panel-heading">
      <p id="liiga-clubs-title">CLUBS / 17</p>
      <p>CUSTOM CURRENT GLYPHS</p>
    </div>

    <div class="liiga-club-grid">
      {
        LIIGA_TEAMS.map((team) => (
          <div class="liiga-club-mark" data-liiga-club-mark={team.id}>
            <svg
              viewBox="0 0 48 48"
              role="img"
              aria-label={`${team.name} custom team glyph`}
              data-liiga-mark-id={team.id}
            >
              {
                getLiigaMarkPaths(team.id).map((path, pathIndex) => (
                  <path
                    d={path}
                    class={pathIndex === 0 ? 'liiga-team-mark__shape' : 'liiga-team-mark__shape liiga-team-mark__detail'}
                  ></path>
                ))
              }
            </svg>
            <span>{team.name}</span>
          </div>
        ))
      }
    </div>
  </section>

  <footer class="liiga-footer">
    <p>
      DATA /
      <a href="https://liiga.fi/" target="_blank" rel="noopener nofollow">LIIGA</a>
      · AUTO REFRESH 5 MIN / 30 SEC LIVE
    </p>
    <p>CUSTOM GLYPHS / NOT OFFICIAL CLUB LOGOS</p>
    <p class="liiga-error" data-liiga-error hidden>
      Liiga data unavailable.
      <button type="button" data-liiga-retry>Retry</button>
    </p>
  </footer>
</section>

<script>
  import { initCurrentLiiga } from '../../features/current/liiga';
  initCurrentLiiga();
</script>

<style>
  .current-liiga {
    --liiga-rule: color-mix(in srgb, var(--line-soft) 56%, transparent);
    padding: 30px 0 8px;
  }

  .liiga-mark-sprite {
    position: absolute;
    width: 0;
    height: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .liiga-heading,
  .liiga-panel-heading,
  .liiga-footer,
  .liiga-row,
  .liiga-status-card,
  .liiga-live-card,
  .liiga-club-mark,
  .liiga-next-home {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    text-transform: uppercase;
  }

  .liiga-heading {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 24px;
    margin-bottom: 28px;
  }

  .liiga-index,
  .liiga-source-state,
  .liiga-panel-heading p,
  .liiga-footer p,
  .liiga-status-card p,
  .liiga-live-card p {
    margin: 0;
  }

  .liiga-index {
    color: var(--moss);
    font-size: 0.56rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.09em;
  }

  .liiga-heading h2 {
    margin: 8px 0 0;
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-size: clamp(2.8rem, 6vw, 5.4rem);
    font-weight: 400;
    line-height: 0.9;
    letter-spacing: -0.045em;
  }

  .liiga-source-state {
    color: var(--stone-light);
    font-size: 0.48rem;
    letter-spacing: 0.06em;
    white-space: nowrap;
  }

  .current-liiga.is-live .liiga-source-state {
    color: var(--moss-deep);
  }

  .liiga-ilves,
  .liiga-standings-section,
  .liiga-clubs,
  .liiga-footer {
    border-top: 1px solid var(--liiga-rule);
  }

  .liiga-ilves,
  .liiga-standings-section,
  .liiga-clubs {
    padding: 18px 0 24px;
  }

  .liiga-panel-heading {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 18px;
    margin-bottom: 14px;
    color: var(--stone-light);
    font-size: 0.5rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.07em;
  }

  .liiga-panel-heading p:first-child {
    color: var(--stone);
  }

  .liiga-visually-hidden {
    position: absolute !important;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px !important;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .liiga-status-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    border-top: 1px solid var(--liiga-rule);
    border-left: 1px solid var(--liiga-rule);
  }

  .liiga-status-card {
    min-width: 0;
    min-height: 238px;
    padding: 17px 18px 18px;
    border-right: 1px solid var(--liiga-rule);
    border-bottom: 1px solid var(--liiga-rule);
  }

  .liiga-status-card__header,
  .liiga-live-card__header {
    display: flex;
    justify-content: space-between;
    gap: 14px;
    color: var(--stone-light);
    font-size: 0.45rem;
    font-weight: 600;
    letter-spacing: 0.065em;
  }

  .liiga-status-card__header span:first-child,
  .liiga-live-card__header span:first-child {
    color: var(--stone);
  }

  .liiga-match-board {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: center;
    gap: 12px;
    margin-top: 28px;
  }

  .liiga-match-team {
    display: flex;
    min-width: 0;
    align-items: center;
    justify-content: flex-end;
    gap: 9px;
    color: var(--stone);
  }

  .liiga-match-team--away {
    justify-content: flex-start;
  }

  .liiga-match-team.is-ilves {
    color: var(--moss-deep);
  }

  .liiga-match-team__name {
    overflow: hidden;
    min-width: 0;
    color: var(--ink-soft);
    font-size: 0.68rem;
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: 0.025em;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .liiga-match-team.is-ilves .liiga-match-team__name {
    color: var(--moss-deep);
  }

  .liiga-match-team__mark {
    width: 42px;
    height: 42px;
    flex: 0 0 auto;
    overflow: visible;
    color: inherit;
  }

  .liiga-match-center,
  .liiga-status-card__position,
  .liiga-live-card__score {
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-weight: 400;
    line-height: 0.82;
    letter-spacing: -0.055em;
  }

  .liiga-match-center {
    font-size: clamp(2.8rem, 4.5vw, 4.4rem);
    white-space: nowrap;
  }

  .liiga-match-center--vs {
    color: var(--stone);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.72rem;
    font-weight: 700;
    line-height: 1;
    letter-spacing: 0.08em;
  }

  .liiga-status-card__kickoff {
    margin-top: 22px !important;
    color: var(--ink);
    font-family: "Times New Roman", Times, serif;
    font-size: clamp(1.45rem, 2.5vw, 2.25rem);
    line-height: 0.95;
    letter-spacing: -0.03em;
    text-align: center;
  }

  .liiga-status-card__position {
    margin-top: 35px !important;
    font-size: clamp(3.8rem, 7vw, 6.4rem);
  }

  .liiga-status-card__meta {
    margin-top: 16px !important;
    color: var(--stone-light);
    font-size: 0.46rem;
    letter-spacing: 0.065em;
  }

  .liiga-status-card__meta--match {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 5px;
    text-align: center;
  }

  .liiga-next-home {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 5px 10px;
    align-items: baseline;
    margin-top: 18px;
    padding-top: 10px;
    border-top: 1px solid var(--liiga-rule);
    color: var(--stone-light);
    font-size: 0.42rem;
    line-height: 1.25;
    letter-spacing: 0.055em;
  }

  .liiga-next-home[hidden] {
    display: none;
  }

  .liiga-next-home strong {
    overflow: hidden;
    min-width: 0;
    color: var(--ink-soft);
    font-size: 0.5rem;
    font-weight: 700;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .liiga-next-home span:last-child {
    grid-column: 1 / -1;
  }

  .liiga-status-card--position {
    background: color-mix(in srgb, var(--moss) 5%, transparent);
  }

  .liiga-status-card--position .liiga-status-card__position {
    color: var(--moss-deep);
  }

  .liiga-live-card {
    margin-bottom: 14px;
    padding: 18px 20px 24px;
    border: 1px solid var(--moss-deep);
    background: color-mix(in srgb, var(--moss) 7%, transparent);
  }

  .liiga-live-card[hidden] {
    display: none;
  }

  .liiga-live-card__header,
  .liiga-live-card__header span:first-child {
    color: var(--moss-deep);
  }

  .liiga-match-board--live {
    margin-top: 20px;
    gap: 18px;
  }

  .liiga-match-board--live .liiga-match-team__mark {
    width: 54px;
    height: 54px;
  }

  .liiga-match-board--live .liiga-match-team__name {
    font-size: 0.78rem;
  }

  .liiga-live-card__score {
    color: var(--moss-deep);
    font-size: clamp(4.6rem, 9vw, 8rem);
    white-space: nowrap;
  }

  .liiga-table {
    border-top: 1px solid var(--liiga-rule);
    border-bottom: 1px solid var(--liiga-rule);
  }

  .liiga-row {
    display: grid;
    grid-template-columns: 30px minmax(170px, 1fr) repeat(7, minmax(42px, 0.34fr));
    align-items: center;
    min-height: 44px;
    border-bottom: 1px solid var(--liiga-rule);
    color: var(--stone);
    font-size: 0.68rem;
    font-variant-numeric: tabular-nums;
    letter-spacing: 0.02em;
  }

  .liiga-row:last-child {
    border-bottom: 0;
  }

  .liiga-row > span {
    min-width: 0;
    padding: 0 8px;
    text-align: right;
  }

  .liiga-row > span:first-child {
    padding-left: 2px;
    text-align: left;
  }

  .liiga-row--header {
    min-height: 31px;
    color: var(--stone-light);
    font-size: 0.54rem;
    font-weight: 600;
    letter-spacing: 0.055em;
  }

  .liiga-row--header > span:nth-child(2) {
    text-align: left;
  }

  .liiga-team {
    display: flex;
    align-items: center;
    gap: 9px;
    text-align: left !important;
  }

  .liiga-team-mark,
  .liiga-club-mark svg {
    width: 30px;
    height: 30px;
    flex: 0 0 auto;
    color: var(--stone);
  }

  .liiga-team-mark svg,
  .liiga-club-mark svg {
    display: block;
    width: 100%;
    height: 100%;
    overflow: visible;
  }

  :global(.liiga-team-mark__shape) {
    fill: none;
    stroke: currentColor;
    stroke-width: 1.6;
    stroke-linecap: round;
    stroke-linejoin: round;
    vector-effect: non-scaling-stroke;
  }

  :global(.liiga-team-mark__detail) {
    stroke-width: 1.15;
    opacity: 0.78;
  }

  .liiga-team__name {
    overflow: hidden;
    color: var(--ink-soft);
    font-weight: 600;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .liiga-points {
    color: var(--ink);
    font-weight: 700;
  }

  .liiga-row.is-ilves {
    background: color-mix(in srgb, var(--moss) 6%, transparent);
  }

  .liiga-row.is-ilves .liiga-team-mark,
  .liiga-row.is-ilves .liiga-team__name,
  .liiga-row.is-ilves .liiga-points,
  .liiga-row.is-ilves .liiga-rank {
    color: var(--moss-deep);
  }

  .liiga-club-grid {
    display: grid;
    grid-template-columns: repeat(9, minmax(0, 1fr));
    border-top: 1px solid var(--liiga-rule);
    border-left: 1px solid var(--liiga-rule);
  }

  .liiga-club-mark {
    display: grid;
    justify-items: center;
    align-content: center;
    gap: 10px;
    min-width: 0;
    min-height: 112px;
    padding: 18px 8px 14px;
    border-right: 1px solid var(--liiga-rule);
    border-bottom: 1px solid var(--liiga-rule);
    color: var(--stone);
    text-align: center;
  }

  .liiga-club-mark svg {
    width: 48px;
    height: 48px;
  }

  .liiga-club-mark span {
    overflow: hidden;
    width: 100%;
    color: var(--stone-light);
    font-size: 0.4rem;
    letter-spacing: 0.045em;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .liiga-club-mark[data-liiga-club-mark="ilves"] {
    color: var(--moss-deep);
  }

  .liiga-footer {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    justify-content: space-between;
    gap: 10px 24px;
    padding-top: 15px;
    color: var(--stone-light);
    font-size: 0.42rem;
    line-height: 1.5;
    letter-spacing: 0.055em;
  }

  .liiga-footer a,
  .liiga-error button {
    color: var(--stone);
  }

  .liiga-footer a {
    text-decoration: none;
  }

  .liiga-footer a:hover,
  .liiga-footer a:focus-visible,
  .liiga-error button:hover,
  .liiga-error button:focus-visible {
    color: var(--moss-deep);
  }

  .liiga-error {
    flex-basis: 100%;
  }

  .liiga-error button {
    min-width: 24px;
    min-height: 24px;
    padding: 0 2px;
    border: 0;
    background: transparent;
    font: inherit;
    letter-spacing: inherit;
    text-transform: inherit;
    text-decoration: underline;
    cursor: pointer;
  }

  @media (max-width: 760px) {
    .liiga-heading {
      align-items: start;
    }

    .liiga-status-grid {
      grid-template-columns: 1fr;
    }

    .liiga-status-card {
      min-height: 190px;
    }

    .liiga-match-board {
      margin-top: 22px;
    }

    .liiga-status-card__position {
      margin-top: 25px !important;
    }

    .liiga-row {
      grid-template-columns: 26px minmax(110px, 1fr) 36px 38px 42px;
      min-height: 42px;
      font-size: 0.64rem;
    }

    .liiga-row > span {
      padding: 0 4px;
    }

    .liiga-row .liiga-col-secondary {
      display: none;
    }

    .liiga-row > span:nth-child(8) {
      grid-column: 4;
    }

    .liiga-row > span:nth-child(9) {
      grid-column: 5;
    }

    .liiga-team {
      grid-column: 2;
    }

    .liiga-team-mark {
      width: 27px;
      height: 27px;
    }

    .liiga-club-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  @media (max-width: 420px) {
    .liiga-heading h2 {
      font-size: 2.65rem;
    }

    .liiga-source-state {
      font-size: 0.4rem;
    }

    .liiga-status-card__header,
    .liiga-live-card__header {
      font-size: 0.4rem;
    }

    .liiga-match-board {
      gap: 7px;
    }

    .liiga-match-team {
      gap: 5px;
    }

    .liiga-match-team__name {
      font-size: 0.58rem;
    }

    .liiga-match-team__mark {
      width: 34px;
      height: 34px;
    }

    .liiga-match-center {
      font-size: 2.7rem;
    }

    .liiga-match-center--vs {
      font-size: 0.58rem;
    }

    .liiga-status-card__kickoff {
      font-size: 1.65rem;
    }

    .liiga-match-board--live {
      gap: 9px;
    }

    .liiga-match-board--live .liiga-match-team__mark {
      width: 40px;
      height: 40px;
    }

    .liiga-match-board--live .liiga-match-team__name {
      font-size: 0.62rem;
    }

    .liiga-live-card__score {
      font-size: 4.1rem;
    }

    .liiga-club-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .liiga-club-mark {
      min-height: 104px;
    }

    .liiga-club-mark svg {
      width: 44px;
      height: 44px;
    }
  }
</style>
